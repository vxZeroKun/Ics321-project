class Tournament {
  String name;
  int id;
  String startDate;
  String endDate;
  Tournament(
      {required this.endDate,
      required this.id,
      required this.name,
      required this.startDate});
}
